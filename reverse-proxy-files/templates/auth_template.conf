<Location /auth/>
ProxyPass "http://$EXTERNAL_IP:8080/auth/"
ProxyPassReverse "http://$EXTERNAL_IP:8080/auth/"
</Location>

# make this configurable to disable access to the admin interface via switch in values
#

# admin is not working
<Location /admin/>
ProxyPass "http://$EXTERNAL_IP:8080/admin/"
ProxyPassReverse "http://$EXTERNAL_IP:8080/admin/"
</Location>

<Location /atlas/auth/>
ProxyPass "http://$EXTERNAL_IP:8080/auth/"
ProxyPassReverse "http://$EXTERNAL_IP:8080/auth/"
</Location>

# summary:
# localhost or 127.0.0.1 dont work
# needs the ip I define in the docker compose script